node_modules/
*.tgz

# dist/ is committed on purpose: `npm install -g github:...` does not install
# typescript, so there is nothing to build with at install time. Run
# `npm run build` before committing source changes. CI checks it is current.

# Secrets. Never commit these.
.env
.env.*
!.env.example

# Generated clips, and the local avatar library from before it moved to ~/.ugc.
out/
refs/
avatars.json
.ugc-pulled.json
.paper-sync/

.DS_Store
